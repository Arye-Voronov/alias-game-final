# מדריך התקנת Ollama והמודל

המדריך מיועד למי שכבר הוריד את הקוד של המשחק ורוצה להפעיל את רמזי ה-AI.

## 1. מתקינים Ollama

נכנסים לאתר:

```text
https://ollama.com/download
```

מורידים לפי המחשב:

- ב-Mac לוחצים על `Download for macOS`
- ב-Windows לוחצים על `Download for Windows`

מתקינים את Ollama ופותחים אותו פעם אחת.

## 2. בודקים ש-Ollama עובד

פותחים Terminal ב-Mac או PowerShell ב-Windows ומריצים:

```bash
ollama -v
```

אם מופיע מספר גרסה, Ollama מותקן.

אם כתוב שהפקודה לא מוכרת, סוגרים ופותחים מחדש את הטרמינל ומנסים שוב.

## 3. מורידים את המודל

מריצים:

```bash
ollama run gemma3:4b
```

מחכים שההורדה תסתיים. זה יכול לקחת כמה דקות.

אם נפתח צ'אט אחרי ההורדה, יוצאים ממנו עם:

```text
/bye
```

## 4. בודקים שהמודל ירד

מריצים:

```bash
ollama list
```

צריך לראות ברשימה:

```text
gemma3:4b
```

## 5. מפעילים את המשחק

מתוך תיקיית הפרויקט מריצים:

```bash
python "normal code.py"
```

אם זה לא עובד, מנסים:

```bash
python3 "normal code.py"
```

## 6. משתמשים ברמזי AI

בתוך המשחק:

1. בוחרים קטגוריה.
2. בוחרים `AI משפר רמזים מוכנים`.
3. לוחצים `התחל משחק`.

אם Ollama והמודל עובדים, המשחק ישתמש ברמזי AI.

אם משהו לא עובד, המשחק ישתמש ברמזים המוכנים מראש.

## תקלות נפוצות

### `ollama` לא מוכר

סוגרים ופותחים מחדש את הטרמינל.

אם זה עדיין לא עובד, מתקינים את Ollama שוב מהאתר:

```text
https://ollama.com/download
```

### המודל לא מופיע ב-`ollama list`

מורידים אותו שוב:

```bash
ollama run gemma3:4b
```

### הרמזים איטיים

המודל הראשון אחרי הורדה יכול להיות איטי יותר, ואחר כך בדרך כלל מגיב מהר יותר.

אפשר לשחק בלי AI עם האפשרות `רמזים מוכנים מראש`.
